# 모타 Design System

## 1. 제품 계약

`모타`는 서울 버스 정류장 또는 지하철역을 선택하고, 지금부터 가장 가까운
도착 정보를 다다음 차량까지 확인하는 단일 목적 앱이다.

사용자가 수행하는 일은 네 가지뿐이다.

1. `출근` 또는 `퇴근` 문맥을 선택한다.
2. `버스` 또는 `지하철`을 선택한다.
3. 지도에서 정류장이나 역을 저장하고, 확인할 지점을 선택한다. 정류장은
   최대 네 곳까지 동시에 선택해 함께 볼 수 있다.
4. 지하철은 노선과 상·하행을 선택한 뒤 최대 세 건의 도착 시간을 확인한다.

다음 기능은 제품 범위에 포함하지 않는다.

- 회사·집 주소와 이름 있는 장소 관리
- 순서가 있는 통근 절차, 경유지, 도보·이동 시간 계산
- 출발 시각·도착 예정 시각 계산
- 노선 즐겨찾기와 별도 즐겨찾기 보드
- 여러 교통수단을 연결한 여정 계획

기존 단일 저장 데이터의 정류장과 역은 출근·퇴근 양쪽에 복제해 보존한다.
이후의 추가·선택·삭제는 현재 문맥에만 반영한다.

### 성공 기준

1. 첫 사용자가 두 번의 주요 선택 안에 정류장 또는 역 찾기를 시작한다.
2. 저장한 정류장과 역은 다음 방문에도 유지된다.
3. 선택한 각 정류장의 버스 또는 선택한 역·방향의 지하철을 정류장마다
   최대 세 건만 보여준다.
4. 현재 선택, 도착 시간, 운행 방향을 색상 없이도 구분할 수 있다.
5. 360px 모바일과 960px 이상 데스크톱에서 가로 스크롤 없이 사용할 수 있다.
6. 네트워크 실패 시 기존 선택을 지우지 않고 같은 위치에서 다시 시도할 수 있다.
7. 출근과 퇴근의 버스 정류장·지하철역 선택은 서로 변경하지 않고 독립 저장된다.

## 2. 시각 방향

**Urban utility minimalism.** 대중교통 전광판의 즉시성과 편집 도구의 절제된
구조를 결합한다. 지도는 위치 맥락을 제공하고, 숫자로 표시한 도착 시간이
시각적 주인공이다.

- 한 화면에 하나의 주요 질문만 보여준다.
- 검정·백색·라임 신호색을 사용하고, 지하철 구분에만 보라색을 사용한다.
- 섹션은 중첩 카드가 아니라 여백과 1px 구분선으로 나눈다.
- 그라디언트, 유리 효과, 장식 일러스트, 과도한 둥근 모서리와 그림자를 쓰지 않는다.
- 아이콘만으로 의미를 전달하지 않고 짧은 한국어 레이블을 함께 제공한다.

## 3. 토큰

### 색상

| 토큰 | 값 | 용도 |
|---|---:|---|
| `--ink` | `#0b0b0b` | 기본 글자, 활성 컨트롤 |
| `--paper` | `#f7f7f3` | 앱 배경 |
| `--surface` | `#ffffff` | 패널과 선택 행 |
| `--line` | `#d9d9d2` | 구분선 |
| `--line-strong` | `#a8a8a0` | 빈 상태와 강조 경계 |
| `--muted` | `#62625d` | 보조 정보 |
| `--signal` | `#c7f000` | 현재 선택과 주요 행동 |
| `--signal-soft` | `#f4fbd6` | 선택 행 배경 |
| `--signal-ink` | `#182000` | 신호색 위 글자 |
| `--route-blue` | `#155eef` | 버스와 지도 포커스 |
| `--subway` | `#7c3aed` | 지하철 정체성 |
| `--danger` | `#c81e1e` | 오류 |
| `--danger-ink` | `#721c1c` | 오류 글자 |
| `--danger-surface` | `#fff2f0` | 오류 배경 |
| `--on-ink` | `#ffffff` | 어두운 면 위 글자 |
| `--map-base` | `#e6e9e1` | 지도 타일 뒤 배경 |
| `--skeleton-base` | `#eeeeea` | 로딩 기본 면 |
| `--skeleton-raised` | `#deded8` | 로딩 펄스 면 |
| `--scrim` | `rgb(11 11 11 / 48%)` | 선택기 배경막 |

색상은 상태를 단독으로 전달하지 않는다. 활성 상태는 `aria-selected` 또는
`aria-pressed`, 글자 굵기, 경계와 신호 스트립을 함께 사용한다.

### 글자

- 기본: `"Pretendard Variable", Pretendard, "Helvetica Neue", system-ui, sans-serif`
- 브랜드: `clamp(1.5rem, 3vw, 2.25rem)`, 780
- 섹션 제목: `1.125rem`, 720
- 본문: `0.9375rem`, 450, 줄 높이 1.5
- 도착 시간: `clamp(1.75rem, 5vw, 3rem)`, 780, tabular numerals
- 메타 정보: `0.75rem`, 650, 자간 `0.05em`

### 간격과 형태

- 4px 기준 배수만 사용한다: 4, 8, 12, 16, 24, 32.
- 컨트롤의 최소 높이는 44px이다.
- 컨트롤 반지름은 8px, 모바일 시트 상단은 18px이다.
- 정보 행은 반지름 없이 구분선으로 이어 붙인다.
- 그림자는 떠 있는 지도 컨트롤과 모바일 시트에만 허용한다.

## 4. 반응형 구조

이 화면은 StyleGallery의
[`list-detail`](https://github.com/changeroa/StyleGallery/blob/main/patterns/split-sidebar/list-detail.md)과
[`scroll-body-shell`](https://github.com/changeroa/StyleGallery/blob/main/patterns/viewport-shell/scroll-body-shell.md)
구조를 결합한다.

### 데스크톱: 960px 이상

- 앱 셸은 `100dvh` 높이의 420px 제어 레일과 나머지 지도 영역으로 나뉜다.
- 문서는 스크롤하지 않는다.
- 브랜드와 교통수단 탭은 레일 위에 고정한다.
- 지점 목록과 도착 정보가 들어 있는 `.rail-scroll`만 세로 스크롤을 소유한다.
- 지도는 선택 지점을 계속 보여주며 레일 스크롤의 영향을 받지 않는다.
- 지점 찾기를 시작해도 별도 대화상자를 열지 않는다. 같은 지도 위에 검색 도구와
  후보 목록을 얹어 위치 맥락과 지도 중심을 그대로 유지한다.

### 모바일: 960px 미만

- 앱 셸은 `100dvh` 세로 구조다.
- 지도는 초기 화면에서 렌더링하지 않고 아래 시트가 전체 높이를 사용한다.
- 사용자가 브랜드 헤더 아래의 `지도 열기`를 누르면 지도는 위쪽 55dvh에 열리고,
  지도 위의 `지도 닫기`를 누르면 다시 완전히 닫힌다.
- 지점 찾기 중에는 지도를 55dvh로 즉시 확장해 후보 마커와 선택 도구를 함께
  조작할 공간을 확보하고, 저장하거나 취소하면 이전 높이로 돌아간다.
- 아래 시트가 주 작업 표면이며 자체 세로 스크롤을 소유한다.
- 브랜드, 출퇴근 문맥, 교통수단 탭, 선택 목록, 도착 정보 순으로 한 열에 배치한다.
- 출퇴근 문맥 전환은 시트 스크롤 상단에 붙어 현재 편집 대상을 항상 보여준다.
- 360px에서 모든 기본 내용이 줄바꿈되고 가로 스크롤이 생기지 않는다.
- 저장 지점 목록은 모바일에서 최대 두 행 높이만 차지하고, 그보다 많으면
  목록 안에서 스크롤해 도착 정보가 화면 아래로 밀리지 않게 한다.
- 모바일에서는 목록을 항상 제공하고 지도는 필요할 때만 여는 위치 확인
  표면으로 사용한다. 목록은 키보드와 스크린 리더의 선택 대안이다.

모든 스크롤 자식은 `min-block-size: 0`을 가져야 하며, 긴 역명과 방향은
`overflow-wrap: anywhere`로 셸을 밀어내지 않는다.

## 5. 컴포넌트 계약

### 브랜드 헤더

- `모타`와 제품명에 답하는 질문형 후크 `지금, 뭐 타?`만 보여준다.
- 브랜드 마크는 설치 아이콘과 같은 `/pwa-icon.svg` 경로 마크를 사용해
  홈 화면, 브라우저 탭, 설치 앱의 시각 정체성을 일치시킨다.
- 설치, 로그인, 설정과 같은 보조 행동을 헤더에 추가하지 않는다.

### 출퇴근 문맥 탭

- 정확히 `출근`, `퇴근` 두 탭을 제공하고 각각 독립된 버스 정류장 목록과
  지하철역 선택을 가진다.
- 각 탭은 저장된 버스 정류장 수와 지하철 설정 여부를 짧게 요약한다.
- 활성 문맥은 검정 면, 라임 하단 신호선, `aria-selected`를 함께 사용한다.
- `tablist`, `tab`, 좌우 방향키 이동을 지원하며 문맥 전환만으로 저장값을
  변경하거나 두 문맥을 합치지 않는다.
- 모바일에서는 시트의 스크롤 상단에 고정해 도착 정보까지 내려가도 현재
  출근·퇴근 문맥을 잃지 않게 한다.

### 교통수단 탭

- 정확히 `버스`, `지하철` 두 탭만 제공한다.
- `tablist`, `tab`, `aria-selected`, 방향키 이동을 지원한다.
- 선택을 바꾸면 해당 교통수단의 최근 선택을 복원한다.
- beui `tabs`의 공유 활성 표시 원리를 참고하되, 프로젝트에는 새 모션
  의존성을 추가하지 않고 CSS 색상·경계 전환만 사용한다.

### 지점 선택 목록

- 섹션은 `어디서 탈까요?`라는 질문 뒤에 `버스 정류장` 또는 `지하철역`을
  제목으로 보여주며, 행동 버튼은 `정류장 찾기` 또는 `역 찾기`라고 쓴다.
- 버스 행은 정류장명과 5자리 ARS 번호를 보여준다.
- 지하철 행은 역명과 노선을 보여준다.
- 행 전체가 44px 이상의 선택 버튼이며 `aria-pressed`를 사용한다.
- 현재 선택에는 3px 신호 스트립과 `CheckCircle2` 아이콘, `지금 보는 곳`
  보조 텍스트가 있다.
- 삭제는 독립 버튼이며 선택 버튼 안에 중첩하지 않는다.
- 찾기 행동은 `MapPinPlus` 아이콘을 사용한다. 빈 상태는 교통수단 아이콘과
  최대 3건을 볼 수 있다는 효용 문장만 보여준다.

### 인라인 지도 찾기

- `정류장 찾기` 또는 `역 찾기`를 누르면 현재 지도가 찾기 상태로 전환된다.
  별도 대화상자, 배경막, 두 번째 지도는 만들지 않는다.
- 진입 즉시 현재 지도 중심에서 한 번 검색하고, 이후 지도 이동만으로는 요청하지
  않는다. 사용자가 `이 위치 다시 찾기`를 눌렀을 때만 새 중심으로 검색한다.
- 저장된 지점은 그대로 보이고 검색 후보는 점선 마커로 구분한다. 마커와 지도
  아래의 후보 목록 선택은 동일한 `aria-pressed` 상태를 공유한다.
- 지도 위 도구는 현재 찾는 교통수단, 검색 진행·오류·빈 상태, 선택 수,
  `취소`, `저장`을 텍스트로 제공한다.
- 저장 전에는 기존 지점 목록을 변경하지 않는다. 취소는 후보만 버리고 기존
  지도 중심과 저장값을 보존한다.
- 모바일에서는 찾기 상태가 지도 높이를 55dvh로 확장하며 후보 목록은 지도
  하단의 한 줄 스크롤 영역을 사용한다. 기본 시트와 별도의 세로 스크롤을
  만들지 않는다.

### 지하철 방향 탭

- 도착 데이터에서 실제로 관찰한 `노선 + 상·하행` 조합만 보여준다.
- 상행은 `ArrowUp`, 하행은 `ArrowDown`, 그 밖의 방향은 `ArrowRight`로
  텍스트 의미를 보조한다.
- 가장 이른 도착이 속한 방향을 기본 선택한다.
- 방향을 바꾸면 같은 역의 해당 방향 열차만 최대 세 건 보여준다.
- 행의 종착 방향은 별도 텍스트로 계속 표시한다.

### 도착 정보

- 섹션은 `곧 오는 순서`를 눈썹 문구로 쓰고, 제목은 `정류장명 다음 버스`
  또는 `역명 다음 열차`로 쓴다.
- 버스는 서버에서 정렬한 노선 행 중 최대 세 건을 보여준다.
- 지하철은 선택한 방향으로 필터한 뒤 최대 세 건을 보여준다.
- 각 행은 `1`, `2`, `3` 빠른 순서 표지를 가져 목록의 의미를 즉시 전달한다.
- 첫 도착 시간은 가장 크게, 노선·방향과 남은 정류장 정보는 보조 계층으로 표시한다.
- 로딩, 성공 시각, 빈 상태, 오류와 `다시 시도` 상태를 모두 제공한다.
- 새로고침은 아이콘만 두지 않고 `RefreshCw`와 `새로고침` 레이블을 함께 제공한다.
- 새로고침 중에도 직전 성공 결과가 있다면 지우지 않는다.

## 6. 상호작용과 모션

- 버튼은 누를 때 `transform: scale(0.98)`만 사용한다.
- 선택, 로딩, 오류 전환은 160ms 이하의 색상·불투명도 전환만 사용한다.
- 레이아웃 높이와 위치는 애니메이션하지 않는다.
- `prefers-reduced-motion: reduce`에서는 전환 시간을 1ms로 줄인다.
- 비동기 행동은 버튼의 비활성 상태와 `aria-live` 문장으로 진행 상황을 알린다.

## 7. 접근성

- `main`, `header`, `nav`, `section` 랜드마크를 사용한다.
- 포커스 표시는 최소 2px이며 배경과 3:1 이상 대비를 가진다.
- 터치 대상은 44x44px 이상이다.
- 오류는 색상과 함께 텍스트와 `role="alert"`를 사용한다.
- 지도 마커만으로 선택을 강제하지 않는다.
- 실시간 영역은 전체 목록을 반복 낭독하지 않고 상태 문장만 `aria-live`로 갱신한다.

## 8. QA 기준

다음 상태를 모바일 360x800, 태블릿 768x1024, 데스크톱 1440x900에서 확인한다.

- 저장 지점 없음
- 버스 정류장 선택과 최대 세 행
- 지하철역 선택, 두 방향 전환과 방향별 최대 세 행
- 현재 지도에서 버스·지하철 후보 검색, 선택, 취소, 저장
- 로딩, 빈 응답, 네트워크 오류, 다시 시도
- 30자 이상의 정류장·역·방향 이름
- 키보드만 사용한 탭, 지점, 방향, 새로고침 이동
- `prefers-reduced-motion` 환경
- 출근·퇴근별 버스/지하철 저장, 전환 후 복원, 한쪽 삭제 시 다른 쪽 보존

## 9. 서울 실시간 3D 지도

`/3d-preview`는 서울 지하철 노선·역과 조건부 버스 노선·정류장 위에 현재 운행
차량을 표시하는 관찰용 화면이다. 기존 `/` 화면의 저장 지점, 출퇴근 문맥, 도착
정보와 인증 상태는 읽거나 변경하지 않는다.

### 화면 구조

- 960px 이상에서는 왼쪽 420px 제어 레일과 나머지 3D 지도를 `100dvh` 안에
  배치한다. 레일만 독립적으로 세로 스크롤한다.
- 960px 미만에서는 지도를 위에, 제어 레일을 아래 스크롤 시트로 배치한다.
  360x800과 768x1024에서도 문서 가로 스크롤이 생기지 않는다.
- 레일은 돌아가기 링크, 연결 상태, `지하철`·`버스` 토글, 화면 요약, 선택한
  지점과 기본적으로 접힌 전체 지점 목록을 제공한다.
- `모타로 돌아가기` 링크는 로딩, 일부 리소스 오류, 치명적 지도 오류 상태에서도
  항상 표시한다.

### 지도 레이어와 선택

- 대량 지점은 개별 DOM 마커가 아니라 MapLibre GeoJSON 소스와 레이어로 그린다.
  순서는 지하철 노선, 버스 노선, 지하철역과 줌별 라벨, 버스 정류장과 줌별 라벨,
  지하철 차량, 버스 차량, 선택 강조다.
- 지하철역은 역사·지붕·입구, 정류장은 쉘터·표지판, 지하철과 버스는 차체·창문·
  지붕·전면을 조합한 입체 오브젝트로 표시한다. MapLibre `fill-extrusion`을 사용하고
  별도 3D 엔진이나 DOM 차량 마커는 추가하지 않는다.
- 역·정류장은 실제 지점 좌표에 고정하고 인접 노선 방향에 맞춰 배치한다. 모델의
  크기와 외관은 식별을 위한 축약 표현이며 실제 건물 외형이나 지하 깊이를 재현하지
  않는다. 노선명과 방향 텍스트도 함께 제공한다.
- 지도 피처 선택은 하나의 접근 가능한 팝업과 레일 상세 상태를 동기화한다. 같은
  지점을 담은 접힌 DOM 목록이 키보드·스크린 리더 대안이며 팝업 종료 후 목록에서
  시작한 포커스는 해당 행으로 돌아간다.
- 지도 확대·축소·방향 컨트롤과 attribution은 계속 표시하며 조작 대상은 최소
  44x44px로 유지한다.

### 실시간 상태와 모션

- 지하철 실시간 위치는 공식 역 기준 데이터를 노선 구간에 대응시킨 값이며 상세에
  `역 구간 기준 실시간 위치`라고 밝힌다. 버스는 공식 GPS 위치를 사용한다.
- 서버가 보낸 완전한 차량 스냅샷만 교체해 표시한다. 새 스냅샷 사이를 짧게
  노선을 따라 800ms 동안 보간하지만 시간표나 모의 차량은 만들지 않는다. 회전은
  이동 구간 방향을 따르고, 정차 중에는 기존 방향을 유지한다. 오브젝트 갱신은 최대
  초당 30회이며 노선 탐색은 스냅샷당 한 번 준비한다.
- 같은 노선에서 두 관측점을 연결할 수 없거나, 관측 간격이 90초를 넘거나, 위치가
  허용 오차 또는 이동 거리 한계를 벗어나면 보간하지 않고 최신 관측점으로 갱신한다.
  보간을 허용하는 노선과 관측점의 거리는 버스 20m, 지하철 40m 이내이며, 작은
  좌표 차이는 시작과 끝에서 연속적으로 반영해 노선으로 갑자기 뛰지 않게 한다.
- 소스 장애는 해당 차량을 즉시 지우고 정적 노선·역·정류장은 유지한다. SSE 연결이
  끊기면 두 교통수단 차량을 모두 즉시 지우며 자동 재연결 중임을 텍스트로 알린다.
- 버스는 확대 16 이상, 화면 면적 4km² 이하, 화면 내 노선 40개 이하에서만 켠다.
  조건을 충족하지 않거나 API 키가 없으면 토글과 사유 문구를 유지하되 버스 피처는
  표시하지 않는다.
- 지도 타일과 일부 리소스 오류는 계속 사용할 수 있는 경고로 표시한다. 지도 생성,
  스타일, `building-3d` 레이어, WebGL context 오류는 지도를 대체하는 오류 화면으로
  표시한다.
- `prefers-reduced-motion: reduce`에서는 차량 보간 없이 새 스냅샷으로 즉시 이동하고
  비필수 CSS 전환을 1ms로 줄인다.

이 화면에는 시간 재생, 속도 조절, 혼잡도, 승하차 인원, 날씨, 여정 ETA, 검색,
즐겨찾기 또는 설정 저장을 추가하지 않는다. 그라디언트, 유리 효과, 장식 일러스트와
새 글꼴도 사용하지 않는다.
