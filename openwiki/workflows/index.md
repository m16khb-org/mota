# Files

- [Arrival Display and Refresh](arrival-refresh.md) - How a selected bus stop or subway station becomes the three displayed arrival rows — per-stop request state in useArrivalDetail, the monotonically increasing request counters that discard stale responses, error retention that never erases the last success, the subway direction filter, the receipt-time elapsed countdown, and the never-cached live endpoints behind it.
- [Google Login and Session Verification](authentication.md) - The end-to-end Google login mota coordinates itself — PKCE material in short-lived flow cookies, the callback code exchange, host-only __Host-mota-access/__Host-mota-refresh session cookies, local JWKS verification with server-side refresh rotation relayed back through set-cookie, and best-effort logout.
- [Selections Persistence and Sync](settings-sync.md) - How a saved-selections document persists — the anonymous localStorage lane under mota:transit-selections:v1, authenticated hydration and first-login upload against GET/PUT /api/settings, the serialized save chain with generation/mutation guards, and the compare-and-swap version protocol that turns a stale write into 409 SETTINGS_VERSION_CONFLICT and syncStatus error.
- [Inline Map Stop and Station Finder](stop-discovery.md) - How the existing Leaflet map becomes the bus-stop and subway-station finder — one search at entry at the current center, re-search only on an explicit button, pending candidate selection shared between dashed markers and the result reel, save/cancel preservation rules, and the locate-me accuracy philosophy in locate.ts.
